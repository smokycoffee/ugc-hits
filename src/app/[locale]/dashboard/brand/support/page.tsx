import { BrandDashboardPlaceholder } from "@/components/platform/brand-dashboard-placeholder";

export default function BrandSupportPage() {
  return (
    <BrandDashboardPlaceholder
      eyebrow="Support"
      title="Support"
      description="This support page is wired into the new dashboard navigation so future help, docs, and contact flows can be added without changing the layout again."
    />
  );
}
