import { BrandDashboardPlaceholder } from "@/components/platform/brand-dashboard-placeholder";

export default function BrandAccountsPage() {
  return (
    <BrandDashboardPlaceholder
      eyebrow="Accounts"
      title="Account settings"
      description="Use this section for billing, access, brand profile settings, and future team management. The route is ready even though the UI stays minimal for now."
    />
  );
}
