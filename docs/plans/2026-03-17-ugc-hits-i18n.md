# UGC Hits I18n Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Add bilingual routing to UGC Hits with Polish as the default locale, English at `/en`, and update homepage pricing to `399 PLN`.

**Architecture:** Integrate `next-intl` into the existing App Router project, move homepage content into locale message files, and serve the homepage through a locale-aware route segment. Keep component layouts stable and refactor content sourcing only where needed.

**Tech Stack:** Next.js App Router, TypeScript, Tailwind CSS, next-intl

---

### Task 1: Install and configure next-intl

**Files:**
- Modify: `package.json`
- Modify: `next.config.ts`
- Create: `src/i18n/routing.ts`
- Create: `src/i18n/request.ts`
- Create: `src/proxy.ts`

**Step 1: Install `next-intl`**

**Step 2: Configure the Next.js plugin**

**Step 3: Add locale routing with Polish as default and unprefixed**

**Step 4: Add request config and middleware/proxy**

### Task 2: Move app routing to locale-aware structure

**Files:**
- Create: `src/app/[locale]/layout.tsx`
- Create: `src/app/[locale]/page.tsx`
- Modify: `src/app/layout.tsx`
- Delete or replace: `src/app/page.tsx`

**Step 1: Create locale-aware layout and page routes**

**Step 2: Keep global styles in the root layout**

**Step 3: Move homepage rendering into the locale route**

### Task 3: Add localized message files and message access

**Files:**
- Create: `messages/pl.json`
- Create: `messages/en.json`
- Create: `src/lib/get-landing-content.ts`

**Step 1: Add Polish and English homepage copy**

**Step 2: Replace hardcoded landing content access with translation-backed mapping**

**Step 3: Update pricing to `399 PLN` in both locales**

### Task 4: Add locale-aware header switching

**Files:**
- Modify: `src/components/ui/navbar-menu.tsx`
- Modify: `src/components/landing/site-header.tsx`

**Step 1: Add a language switcher for `PL` and `EN`**

**Step 2: Keep the current section navigation intact**

### Task 5: Verify the bilingual homepage

**Files:**
- Modify as needed based on verification output

**Step 1: Run `npm run lint`**

**Step 2: Run `npm run build`**

**Step 3: Fix issues and re-run verification**
